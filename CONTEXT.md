# 光伏功率监控上下文

本上下文定义光伏功率监控系统中的业务术语，作为采集端、网关、服务端和监控页面共同使用的语言。

## 租户与站点

**租户**：一组相互隔离的用户、光伏站点、测量点、设备和遥测数据的拥有边界。家庭部署可以先只有一个租户。
_避免_：客户（口头称呼，不表达数据隔离边界）、组织树（第一版不引入复杂层级）

**用户/角色**：可以登录监控页面的人员及其权限。`owner` 管理站点、设备和阈值，`viewer` 只读查看趋势和状态。
_避免_：设备账号（设备使用 token，不是页面用户）

## 设备与拓扑

**光伏站点**：一组被监控、具有统一地理位置和配置的光伏发电设备。一个站点可以有一个总功率测量点，也可以扩展到多个组串测量点。
_避免_：项目、场站（除非特指运维组织中的大型场站）

**测量点**：电气拓扑中一个明确的功率观测位置，例如逆变器交流输出或某个直流组串。
_避免_：传感器、通道（传感器是硬件，通道是实现细节）

**采集节点**：安装在测量点附近、负责与电表进行 RS485 通信并通过 Wi-Fi 上传原始电表帧的低压设备；它不负责解释电表业务数据。
_避免_：采集模块（可在口头交流中使用，但文档统一用采集节点）

**接收网关**：可选的 BLE Central 设备，连接一个或多个采集节点，并通过家庭 Wi-Fi 把数据上传到服务端；Wi-Fi 直连方案不需要它。
_避免_：接收模块、服务器（网关不负责持久化业务数据）

**上行批次**：ESP32 采集节点或接收网关一次向服务端提交的一组带设备身份、采集时间、序列号和原始电表帧的记录。
_避免_：消息、包（消息/包属于传输层概念）

**电表原始帧**：采集节点从电表 RS485 总线收到或发出的、保持字节顺序不变的 DL/T 645-2007 报文。原始帧是诊断和后续解析的事实来源，不等同于瞬时功率。
_避免_：电表数据（过于笼统）、已解析测量

**帧解析结果**：服务端根据电表原始帧解码出的业务测量值，例如瞬时功率、累计电量、电压和电流。
_避免_：采集节点测量（采集节点只转发原始帧）

## 测量与状态

**瞬时功率**：某一采样时刻的有功功率，单位为 W。第一版趋势图的主指标是 `power_w`。
_避免_：发电量（发电量是能量，单位为 Wh 或 kWh）

**累计电量**：从电能表或边缘积分得到的能量读数，单位为 Wh。它用于日报和断线后的校正，不等同于瞬时功率。
_避免_：总功率、发电功率（需要区分时使用瞬时功率）

**数据质量**：对测量记录可信程度的标记，例如 `ok`、`estimated`、`invalid`、`offline_gap`。
_避免_：在线状态（在线状态描述连接，不描述测量值可信度）

**在线状态**：网关或采集节点在最近心跳/测量时间窗内是否可通信。
_避免_：数据质量、健康度

**有效测量记录**：通过时间、数值范围、设备身份和序列号校验，可进入趋势查询的记录。
_避免_：原始包（原始包可保留用于诊断，但不直接用于报表）

## 服务与告警

**遥测记录**：服务端根据电表原始帧解析并验证后的某个测量点在某个时间的业务测量数据及其元数据。
_避免_：日志、事件（日志是诊断输出，事件是状态变化）

**设备心跳**：采集节点或接收网关定期上报的通信、信号、电源和固件状态记录。
_避免_：遥测记录（心跳可以没有功率值）

**功率告警**：由服务端根据站点日照时段、功率阈值或持续时间规则产生的异常通知。
_避免_：报警（告警是可确认、可关闭的业务对象；报警泛指硬件信号）
